basic box layout 

with a button to begin

onlick event triggers the sequence of prompts

prompts store variables 

var length- "choose how long you would like your password to be between 8-128characters"
var uppercase- "would you like to include uppercase letters?"
var lowercase- "would you like to include lowercase letters?"
var numeric- "would you like to include numbers?"
var specialCharacters- "would you like to include special characters?"

generate password

for loop (length) {
    if (uppercase === true)
    var ucLetter
}

create variables
var upcaseChoose = "ABCDEFGHIJKLMNOPQRSTUVWXYNZ"
var lowcaseChoose = "abcdefghijklmnopqrstuvwxyz"
var numchoose = "123456789"
var speccharChoose = "!@#$%^&*()_-+=|\{}[]:";'<>?,./~`"

var password = "";

for (var i=0; i<length; i++) {
    var randomNumber = math.floor(math.random() * numchoose.length);
}


